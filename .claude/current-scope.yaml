# Scope contract for EPT-16 -- Policy PDF Download.
# Generated by the planner (definition-of-done skill). Archive to
# .agentic/tickets/EPT-16/ once the PR opens.

ticket: EPT-16
risk: MEDIUM
branch: feature/EPT-16

acceptance_criteria:
  - id: AC-001
    text: "Download button visible next to each policy in the list for authorized users"
  - id: AC-002
    text: "Clicking button downloads a PDF with all six required fields formatted clearly"
  - id: AC-003
    text: "Policyholders can only download their own policies"
  # AC-004 ("Agents can download PDFs for their assigned policies") descoped
  # at Gate 1 approval (2026-09-18): the agent role does not exist yet in
  # this codebase. Human confirmed: implement the download functionality
  # without it. Removed from this ticket's delivered acceptance criteria --
  # see .agentic/tickets/EPT-16/plan.md "Out of scope". Belongs to a future
  # ticket once an agent persona/assignment model is designed.
  - id: AC-005
    text: "Internal staff can download any policy PDF"
  - id: AC-006
    text: "PDF filename includes policy number"
  - id: AC-007
    text: "Download works in Chrome, Firefox, Safari, Edge -- automated proxy (standard Blob/<a download> mechanism, no browser-specific API) plus required manual cross-browser verification"
  - id: AC-008
    text: "(promoted edge case) If policy has no end date (ongoing), PDF shows 'Ongoing'"
  - id: AC-009
    text: "(promoted edge case) If coverage limits are not set, PDF shows 'Not specified'"
  - id: AC-010
    text: "(promoted edge case) If user lacks permission for a policy, download button is hidden, not disabled"
  - id: AC-011
    text: "(promoted edge case) If PDF generation fails, user sees an error message and can retry"

allowed_paths:
  - backend/requirements.txt
  - backend/routers/userpolicies.py
  - backend/routers/admin.py
  - frontend/src/pages/Policies.js
  - frontend/src/pages/Policies.css
  - frontend/src/pages/AdminDashboard.js
  - frontend/src/pages/AdminDashboard.css
  - frontend/src/utils/apiClient.js
  - backend/tests/**
  - frontend/src/**/*.test.js

forbidden_operations:
  - migration
  - deploy
  # dependency_install intentionally removed: this ticket requires adding a
  # PDF generation library (reportlab) to backend/requirements.txt. Flagged
  # explicitly in plan.md as a signal to double check the MEDIUM risk tier
  # at Gate 1, per definition-of-done skill guidance.

frozen_tests: []

verification:
  build: required
  lint: required
  type_check: optional
  unit_tests: required
  integration_tests: required
  api_tests: required
  ui_tests: required
  migration_up: optional
  migration_down: optional

completion:
  all_required_tests_pass: true
  scope_check_passes: true
  every_ac_has_a_passing_tc: true
  # No exception needed: AC-004 was descoped at Gate 1 (2026-09-18), not
  # deferred-but-still-required, so it is no longer in the acceptance_criteria
  # list above and this check applies cleanly to all listed ACs.
