# Copy this file to .env and adjust if needed.
# These defaults match docker-compose.yml at the repo root.

DATABASE_URL=postgresql://covermate:covermate@localhost:5434/insurance_db

# Any long random string works locally.
SECRET_KEY=devsecretkey_change_me

# The user with this email is treated as admin (Admin Dashboard access).
ADMIN_EMAIL=admin@example.com
