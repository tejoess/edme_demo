[pytest]
# EPT-13 test harness. Run from the backend/ directory:  python -m pytest -q
pythonpath = .
testpaths = tests
markers =
    tc: acceptance-criteria test-case id (traceability skill), e.g. @pytest.mark.tc("TC-001")
filterwarnings =
    ignore::DeprecationWarning
